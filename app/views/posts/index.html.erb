<% content_for :title, "게시판" %>

<section class="page-header">
  <div>
    <h1>게시판</h1>
    <p class="text-muted">모든 글을 최신 순으로 확인하세요.</p>
  </div>
  <%= link_to "새 글 쓰기", new_post_path, class: "btn primary" %>
</section>

<div id="posts" class="post-list">
  <% if @posts.any? %>
    <% @posts.each do |post| %>
      <%= render post %>
    <% end %>
  <% else %>
    <p class="empty-state">아직 작성된 글이 없습니다.</p>
  <% end %>
</div>
